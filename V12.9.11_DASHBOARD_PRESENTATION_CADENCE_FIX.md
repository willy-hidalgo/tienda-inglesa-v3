# v12.9.11 Dashboard presentation + cadence placement fix

Dashboard-only refinement. Forecast/model outputs are unchanged.

- Ranking font reduced to 13 px through pandas Styler.
- Non-percent numeric ranking columns use thousands separators while remaining numeric for sorting.
- SKU+Tienda Active ranking shows five rows at a time, keeps all Active rows scrollable, and uses a very light yellow background.
- Added `Rotación` to the global SKU+Tienda ranking.
- Ranking period selector (In-sample/OOS) moved from sidebar to directly above ranking tables.
- Cadence comparison explicitly uses section-level metrics (`unique_id == section`) and was moved next to the section metrics area.
- Removed redundant demo/scenario/store captions and reduced implementation-oriented explanatory text.
- Kept one concise cadence/OOS/forecast-only context line below the selected node title.
