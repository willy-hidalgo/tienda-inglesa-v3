# v12.9.9 — Value Sparse Bias Rescue segmentado

Estado: **diagnóstico, no productivo**.

La policy productiva validada en v12.9.7 permanece congelada. v12.9.9 estudia
únicamente `Valor ($)` para hojas de baja frecuencia (`07-09`, `10-13` días con
venta), porque v12.9.8 mostró mejora en Valor pero no una mejora consistente en
Unidades.

## Cambios

- Segmentación causal: `bucket días venta × método de nivel × flag estacional`.
- Grid de uplift: `1.00, 1.025, 1.05, 1.075, 1.10, 1.125, 1.15`.
- El wMAPE histórico de cada factor se calcula con errores absolutos exactos en
  días positivos; no se aproxima desde totales agregados.
- Gate por segmento: >=8 folds, win-rate >=75%, mediana >0, worst >=-0.50 pp,
  worst |BIAS| worsen <=1.00 pp y weighted gain >0.
- Multi-cutoff replay causal de hasta 4 pseudo-OOS. En cada cutoff se reconstruye
  el selector v11/v12 con bloques más antiguos, se aprende el factor y luego se
  evalúa el bloque held-out.
- El OOS actual nunca participa en el entrenamiento del rescue.
- `valuehat` no se modifica en v12.9.9. La promoción requiere evidencia del
  reporte de aceptación, sección 32.

## Validación

Ejecutar primero el forecast 28d, porque v12.9.9 agrega metadata y errores del grid
por bloque cerrado:

```bash
uv run python app/forecasts.py --update-block-days 28 --n-jobs 8
uv run python -m app.forecasting.validate_v12 --update-block-days 28
uv run python -m app.dashboard_consistency --update-block-days 28
uv run python -m app.forecasting_acceptance_report --update-block-days 28
```

Revisar especialmente las secciones 30, 31 y 32.
