# v12.9.8 — Dashboard coherence build

Dashboard-only correction; the statistical forecast policy and APP_VERSION remain 12.9.8 so an already-valid v12.9.8 `forecast.parquet` does not need to be regenerated.

Changes:
- Actual series rendered as filled area for in-sample and OOS.
- Weekly/monthly aggregation groups by temporal bucket + `period_type`, preventing mixed in-sample/OOS/forecast-only sums at section-specific boundaries.
- Chart rendering defensively clips in-sample, OOS and forecast-only to the section-specific horizon dates.
- New global SKU+Tienda ranking from `metrics.parquet`, independent of main dashboard filters, with both sections and units available.
- Dedicated Excel export for the global ranking.
- Added regression tests for Sec.23-style boundary crossing, date clipping, global ranking scope and area rendering.
