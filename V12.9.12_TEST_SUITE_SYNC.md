# v12.9.12 — Test-suite sync

This package keeps the v12.9.12 production implementation unchanged and synchronizes stale regression contracts with the current code.

## Changes

- Updated current-version assertions from `12.9.11` to `12.9.12`.
- Kept historical v12.9.11 residual-selector diagnostic strings unchanged.
- Updated dashboard global-ranking contract to validate the current vectorized Polars algebra:
  - `sum_abs_error / sum_abs_y`
  - `sum_signed_error / sum_abs_y`
- Updated non-active forecast contract to the generalized OOS/in-sample implementation (`period_type` / `total_col`).
- Updated ranking-period UI contract from the obsolete `select_slider` implementation to the current `segmented_control` implementation.
- Updated dashboard diagnostic-title expectations to v12.9.12.
- Updated the lazy KPI-source contract to the current v12.9.12 wording covering both OOS and in-sample official metric artifacts.
- Added explicit UTF-8 reads to the v12.9.5/v12.9.6 source-contract tests to avoid Windows cp1252 `UnicodeDecodeError`.
- Updated percentage representation contracts to validate numeric percent values passed to Streamlit formatting instead of obsolete preformatted strings.

## Validation in patch environment

- v12.9.12 dedicated contract: `2 passed`.
- Runnable subset covering the originally failing textual/contracts tests: `123 passed`.
- `compileall` for `tests`, `app`, and `settings.py`: OK.
- Full suite could not be executed in the patch container because the global Python environment does not include project dependency `polars`; the user's Windows run already confirms dependency resolution there.

Expected result on the project's normal `uv` environment: the 35 stale-contract/encoding failures reported against v12.9.12 should be eliminated without changing productive forecast logic.
