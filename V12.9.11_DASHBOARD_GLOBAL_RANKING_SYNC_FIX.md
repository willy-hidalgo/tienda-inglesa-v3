# v12.9.11 — Dashboard global ranking metric sync fix

Dashboard-only hotfix. No forecast/model changes.

- SKU+Tienda visible is filtered to the current section and unit, eliminating ambiguity when the same leaf exists in both Unidades and Valor ($).
- Global ranking wMAPE/BIAS are rebuilt from `sum_abs_y`, `sum_abs_error`, and `sum_signed_error`, exactly like the OOS KPI panel.
- The full cross-section/cross-unit ranking remains available in the Excel export.
- BIAS badge text is normal weight (not bold).
