# v12.9.11 dashboard — no activos / formato porcentual

Hotfix visual/artefactos. No modifica modelos ni pronósticos.

- La tabla SKU+Tienda no activos incluye `Pronóstico OOS`, obtenido de `metrics.parquet.sum_yhat`.
- `wMAPE (%)`, `BIAS (%)` y `% ≠0` se muestran con símbolo `%` en las tablas numéricas.
- Los rankings de tienda/SKU conservan sus valores en puntos porcentuales y ahora incluyen `%` explícito en el texto.
- El Excel del ranking global usa formato porcentual literal sobre valores ya expresados en puntos porcentuales, evitando multiplicar por 100 nuevamente.
