# v12.9.11 dashboard sort performance

- Removed the pale-yellow per-cell background from the SKU+Tienda ranking.
- The ranking keeps numeric types and formatting, but avoids the expensive cell-background payload that degraded interactive sorting.
- No forecasting logic or artifacts changed.
