# v12.9.11 — Dashboard compact period/color fix

Presentation-only hotfix. No forecast/model changes.

- Ranking metric period selector changed to a compact segmented control (`OOS` / `In-sample`).
- SKU+Tienda non-percent numeric columns display without thousands separators.
- Reliable light-yellow SKU+Tienda row background restored using a Pandas Styler property.
- Main and non-active charts use separate palettes for in-sample, OOS and forecast-only while keeping actuals as translucent areas and forecasts as high-contrast lines.
