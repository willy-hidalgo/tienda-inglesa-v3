# v12.9.7 corrected

Corrección puntual de la auditoría temporal; no cambia la política productiva ni la versión estadística.

- `APP_VERSION` permanece en `12.9.7` y `ARTIFACT_VERSION` en `20`.
- Sec.23 Valor mantiene el selector productivo v12.9.6 L4 stress-gated y budget 15%.
- El replay multi-cutoff es diagnóstico y estrictamente causal: para validar el bloque `b`, el perfil L4, impacto y peso presupuestal se construyen desde el bloque cerrado `b+1`; los actuals del bloque `b` solo puntúan el resultado.
- El validator limita stress/replay a filas leaf donde la metadata aplica.
- La consistencia familia-final compara `budget_selected AND candidate_available` contra la familia realmente aplicada.
- El reporte de sección 30 selecciona explícitamente filas leaf Sec.23 con replay habilitado.
